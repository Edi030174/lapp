<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="net.lintasarta.permohonan.dao.HVerifikasiDAO">

    <select id="getCountAllHVerifikasi" resultType="int">
        select count(*) from H_IDOSS_VERIFIKASI
    </select>

    <select id="getAllHVerifikasi" resultType="hverifikasi">
        select * from H_IDOSS_VERIFIKASI
    </select>

</mapper>